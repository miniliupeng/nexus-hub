# Nexus-Hub 企业级工程开发与架构约定规范 (Development Convention)

本项目定位为**一线大厂高标的企业级商业化高性能业务中台**，系统化落实微服务底层原理与生产实践。
为确保项目既具备顶级的工业级工程严谨度，又拥有极致的代码可读性与透彻度，开发全流程严格遵守以下核心准则：

---

## 🎯 核心双赢哲学 (The Dual-Goal Engineering Philosophy)

本项目追求以下两者的完美统一，杜绝非此即彼的极端：

1. **对外规格与系统内核（顶级商业中台标准）**：
   - 展现纯正的一线大厂（字节、美团、腾讯、阿里）架构师级水准；
   - 彻底杜绝“教学练习玩具”等自贬标签，在高并发、容灾、事务一致性与编译期依赖注入上严苛达标。
2. **内在体验与通读门槛（返璞归真的极致通透）**：
   - 拒绝晦涩难懂的术语堆砌与装高深的黑话代码；
   - 通过**清晰的代码分层、克制的代码体量与独创的深度透析注释**，让新手与初级工程师也能 100% 毫无门槛地彻底读懂每一个设计决策。

---

## ✍️ 约定一：独创【Why-Flow-Gotcha】三维透析注释规范

代码注释必须兼顾专业深度与直白通俗，对核心结构体、关键方法及高并发逻辑，强制采用 **Why-Flow-Gotcha 三维透析模板**：

```go
// CreateArticle 创建知识资产
//
// 1. 【Why 为什么这么设计】:
//    在高并发发布场景下，如果直接执行 DB Insert，可能因客户端网络超时重试导致 MySQL 产生重复数据；
//    因此采用 X-Idempotency-Key + Redis SetNX 机制做前置排他防御。
//
// 2. 【Flow 底层执行流程】:
//    第一步：从 Context 获取当前登录用户 ID (通过 JWT 中间件解析注入)
//    第二步：开启 GORM 数据库事务 (确保文章入库与统计计数初始化的原子性)
//    第三步：若入库成功则 Commit，若发生任何异常自动触发 defer tx.Rollback()
//
// 3. 【Gotcha 生产避坑】:
//    切勿在事务代码块内执行耗时操作（如调用第三方远程接口），否则会导致 MySQL 行锁长期持有，迅速打满数据库连接池！
func (s *ArticleService) CreateArticle(ctx context.Context, req *dto.CreateArticleReq) error { ... }
```

- **【Why 设计考量】**：说明为什么这么写，解决什么技术痛点，不这么写会发生什么故障；
- **【Flow 执行流程】**：按“第一步、第二步、第三步”用白话理顺底层时序，让阅读者秒建思维模型；
- **【Gotcha 生产避坑】**：显式预警容易引发死锁、内存逃逸、Goroutine 悬挂或连接池打满的危险雷区。

---

## 📜 约定二：模块演进过程沉淀在 `DEV_LOG.md`

每当推进或完成一个核心功能模块，必须在根目录的 **[DEV_LOG.md](./DEV_LOG.md)** 中同步记录其设计与落地细节，格式统一包含以下 4 个小节：

```markdown
### 模块编号与名称 (如：Module 01 - 基础设施容器编排与配置加载引擎)

1. **业务背景与技术痛点 (Problem & Context)**：
   - 为什么要引入该组件？业界标准做法是什么？如果不做会有什么线上故障风险？
2. **核心架构与设计选型 (Architecture & Rationale)**：
   - 模块目录划分、接口抽象设计，为什么选择该技术方案（选型权衡 Trade-off）。
3. **关键代码机制与底层避坑 (Key Implementation & Gotchas)**：
   - 并发安全、锁机制、事务回滚、连接池释放等底层细节与避坑总结。
4. **命令行验证与预期输出 (Verification & CLI)**：
   - 纯终端命令行验证语句（如 curl, make 命令），附带成功的真实输出片段，确保可重复验证。
```

---

## 🏛️ 约定三：工程整洁与代码体量克制原则（Clean Architecture）

1. **杜绝全局变量反模式**：
   - 严禁出现 `global.DB`、`global.Redis`、`global.Config` 等包级全局单例；
   - 必须通过显式构造函数传递依赖，最终交由 **Google Wire** 完成编译期静态装配。
2. **严格单向分层依赖**：
   - 通用基础设施（`pkg/`）：纯技术组件（数据库连接池、JWT 验签、S3 直传凭证），**零业务字段**，严禁反向引用 `internal/`；
   - 表现层（`handler/`）：仅负责入参校验、上下文提取与 DTO 序列化，严禁越过 Service 直接写 SQL；
   - 业务层（`service/`）：负责领域业务规则、状态机流转与事务编排；
   - 仓储层（`repository/`）：负责数据持久化与 Redis 缓存双写，严禁包含业务规则。
3. **代码体量极致精炼（拒绝面条代码）**：
   - 严格遵循单一职责原则（SRP），单个 `.go` 文件代码行数尽量控制在 **100~200 行左右**；
   - 模块逻辑短小精悍，调用链一目了然，方便任何人单步追踪调试（Debug）。

---

## 🧪 约定四：自动化验证准则

1. **终端第一原则**：一切验证通过单元测试、集成测试或命令行脚本进行，严禁自动打开浏览器；
2. **数据竞争检测（Data Race Free）**：所有模块提交前，必须通过 `go test -race ./...` 检查，确保 100% 零并发数据竞争。
